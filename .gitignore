# === OS / filesystem junk ===
.DS_Store
._*
Thumbs.db
desktop.ini
.AppleDouble
.LSOverride

# === Editor / IDE ===
.vscode/
.idea/
*.iml
*.code-workspace
.history/
*.swp
*.swo
*.swn
*~
*.bak
*.orig
*.tmp
Session.vim
.netrwhist

# === Secrets / local env ===
.env
.env.*
!.env.example
.envrc
*.pem
*.key
*.crt
*.p12
secrets/
.secrets/

# === Lua / LuaRocks build artefacts ===
*.rock
*.src.rock
.luarocks/
luarocks/
lua_modules/

# === Logs / runtime artefacts ===
logs/
*.log
*.pid
*.sock
*-auditlog-*.json
nginx-*.conf

# === Downloaded OWASP CRS regression test YAMLs ===
# Populated by crs-regression-test/fetch-tests.sh. Not part of Karna.
crs-regression-test/tests/

# === CRS regression-test runtime outputs ===
# Per-run logs / diffs / category summaries from the bench scripts.
crs-regression-test/results/
crs-regression-test/_excluded/

# === CRS regression-test internal-only artefacts ===
# Local session notes / post-mortems / dev-only tooling we keep for our
# own debugging but do not publish. The public-facing user surface is
# README.md + start.py + fetch-tests.sh + configure-kong.sh +
# bench-coreruleset-fix.sh + run-categories.sh.
crs-regression-test/FINDINGS.md
crs-regression-test/RESIDUAL_FAILS.md
crs-regression-test/toggle-crs-fix.sh
crs-regression-test/bench-coreruleset-fix.sh

# === Docker local state / overrides ===
docker/.data/
docker-compose.override.yml
docker-compose.local.yml

# === Language tooling caches ===
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage/
htmlcov/
node_modules/
dist/
build/

# === Claude Code / AI tooling ===
.claude/

# === Personal / WIP scratchpads ===
TODO.local.md
NOTES.local.md
scratch/
private/
.notes/

# === Performance benchmark harness (private — feeds an eventual blog post) ===
# bench/ contains docker-compose stacks for Apache+ModSec2, nginx+ModSec3
# and Karna side by side, the k6 scenario scripts, the orchestrator,
# raw results, and the ongoing blog-post notes. Not part of Karna's
# public surface and the result data is too noisy to publish raw.
bench/